export async function shareCanvas(canvas,type="image/jpeg",quality=.92){const blob=await canvasToBlob(canvas,type,quality);const file=new File([blob],`photo-effects-${Date.now()}.${type==="image/png"?"png":"jpg"}`,{type});if(navigator.canShare&&navigator.share&&navigator.canShare({files:[file]})){await navigator.share({files:[file],title:"Photo Effects"});return true}return false}export async function downloadCanvas(canvas,type="image/jpeg",quality=.92){const extension=type==="image/png"?"png":"jpg";const fileName=`photo-effects-${Date.now()}.${extension}`;const blob=await canvasToBlob(canvas,type,quality);const url=URL.createObjectURL(blob);const link=document.createElement("a");link.href=url;link.download=fileName;document.body.appendChild(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)}function canvasToBlob(canvas,type,quality){return new Promise(resolve=>{canvas.toBlob(blob=>resolve(blob),type,quality)})}
