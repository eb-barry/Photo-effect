const STORAGE_KEY="photoEffectsSettings";export const DEFAULT_SETTINGS={fontSize:"medium",themeColor:"#0abab5"};export const THEME_COLORS=["#0abab5","#14b8a6","#3b82f6","#8b5cf6","#ec4899","#f97316","#22c55e","#111827"];export function loadSettings(){try{return{...DEFAULT_SETTINGS,...JSON.parse(localStorage.getItem(STORAGE_KEY)||"{}")}}catch{return{...DEFAULT_SETTINGS}}}export function saveSettings(settings){localStorage.setItem(STORAGE_KEY,JSON.stringify(settings));applySettings(settings)}export function applySettings(settings=loadSettings()){document.body.classList.remove("font-small","font-medium","font-large");document.body.classList.add(`font-${settings.fontSize}`);document.documentElement.style.setProperty("--base",settings.themeColor);document.documentElement.style.setProperty("--base-dark",shadeColor(settings.themeColor,-26));document.documentElement.style.setProperty("--base-light",shadeColor(settings.themeColor,72));const meta=document.querySelector('meta[name="theme-color"]');if(meta)meta.setAttribute("content",settings.themeColor)}function shadeColor(hex,percent){const normalized=hex.replace("#","");const num=parseInt(normalized,16);const r=(num>>16)+Math.round(2.55*percent);const g=((num>>8)&0x00ff)+Math.round(2.55*percent);const b=(num&0x0000ff)+Math.round(2.55*percent);return"#"+(0x1000000+clamp(r,0,255)*0x10000+clamp(g,0,255)*0x100+clamp(b,0,255)).toString(16).slice(1)}function clamp(value,min,max){return Math.max(min,Math.min(max,value))}
