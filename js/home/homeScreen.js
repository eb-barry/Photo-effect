import { buildFeatures } from "../config/features.js";import { iconButton } from "../core/iconLoader.js";export function renderHomeScreen(root,navigate){const features=buildFeatures();root.innerHTML=`<main class="app-shell"><header class="home-header"><h1>Photo Effects</h1><p>影像創作工具箱</p>${iconButton({icon:"settings",label:"設定",id:"settingsBtn",className:"home-settings"})}</header><section class="icon-grid" aria-label="功能列表">${features.map(feature=>`<button class="feature-icon" ${feature.enabled?"":"disabled"} data-route="${feature.route||""}" aria-label="${feature.label}"><span class="icon-tile">${feature.icon?`<img src="${feature.icon}" alt="" onerror="this.style.display='none'" />`:""}<span class="icon-fallback">${feature.id}</span></span><span class="icon-label">${feature.label}</span></button>`).join("")}</section></main>`;root.getElementById("settingsBtn").addEventListener("click",()=>navigate("settings"));root.querySelectorAll(".feature-icon[data-route]").forEach(button=>{button.addEventListener("click",()=>{const route=button.dataset.route;if(route)navigate(route)})})}
